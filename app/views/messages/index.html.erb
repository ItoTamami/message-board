<!-- <h1>Messages#index</h1>
<p>Find me in app/views/messages/index.html.erb</p> -->

<!-- ##5.8 メッセージ一覧の表示　で追記 ここから -->
<h1>メッセージ一覧</h1>
<% @messages.each do |message| %>
  名前:<%= message.name %>
  内容:<%= message.body %>
  <br>
<% end %>
<!-- ##5.8 メッセージ一覧の表示　で追記　ここまで -->

<%= form_for(Message.new) do |f| %>
  名前:
  <%= f.text_field :name %>

  内容:
  <%= f.text_field :body %>
  <%= f.submit %>
<% end %>
